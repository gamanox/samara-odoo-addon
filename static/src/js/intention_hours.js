/**
 * Placeholder asset to keep manifest reference valid.
 * Inline scripts in templates handle the schedule fetching on the shop page.
 */
document.addEventListener("DOMContentLoaded", () => {
  // Intentionally left minimal; no-op to avoid missing asset errors.
});
